# Python uses DynamicTyping, which means the variable types are determined and checked at the runtime. 
# Many other languages which uses static typing.
# The value of a variable can be changed from integer to string, because Variables do not have type, only their value does.

car = "Baleno"
print (car)

car = 6695
print (car)
